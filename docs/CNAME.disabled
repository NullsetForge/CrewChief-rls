# If you ever want this on a custom domain (e.g. crewchief.nullsetforge.com), rename this file
# to CNAME and put the bare hostname in it on one line, then add the matching DNS record.
# Left disabled so GitHub serves it from the default nullsetforge.github.io/CrewChief-rls URL.
